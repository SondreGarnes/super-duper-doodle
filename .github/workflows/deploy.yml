name: Build and Deploy

on:
  push:
    branches:
      - main

env:
  IMAGE: ghcr.io/sondregarnes/super-duper-doodle

jobs:
  build-and-deploy:
    runs-on: self-hosted  # runs on your Ubuntu laptop

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ env.IMAGE }}:latest
            ${{ env.IMAGE }}:${{ github.sha }}

      - name: Deploy to cluster
        run: |
          kubectl set image deployment/super-duper-doodle \
            super-duper-doodle=${{ env.IMAGE }}:${{ github.sha }}
          kubectl rollout status deployment/super-duper-doodle
